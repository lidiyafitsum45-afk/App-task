// Fill these in after you create your Supabase project (see README step 1)
// and generate VAPID keys (see README step 4). This file is safe to expose
// publicly — it only contains the anon key, not a secret.
const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-SUPABASE-ANON-KEY";
const VAPID_PUBLIC_KEY = "YOUR-VAPID-PUBLIC-KEY";
